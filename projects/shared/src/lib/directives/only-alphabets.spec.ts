import { OnlyAlphabets } from './only-alphabets';

describe('OnlyAlphabets', () => {
  it('should create an instance', () => {
    const directive = new OnlyAlphabets();
    expect(directive).toBeTruthy();
  });
});
