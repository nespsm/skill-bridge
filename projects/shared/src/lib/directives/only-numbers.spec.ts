import { OnlyNumbers } from './only-numbers';

describe('OnlyNumbers', () => {
  it('should create an instance', () => {
    const directive = new OnlyNumbers();
    expect(directive).toBeTruthy();
  });
});
